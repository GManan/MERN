export const config = {
    mongoDB_Link: 'mongodb://localhost/myDB',
    mongoDB_CAT: 'mongodb://localhost/Cat',
    port: 80,
    httpsPort: 443,
    jwtKey: 'my_secret_key',
    jwtExpirySeconds: 10000
}
